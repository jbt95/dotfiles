---
description: Commit and publish the current branch
---
Commit the current changes with a clear, descriptive message, then push to `origin` on the current feature branch. Do **not** merge to `main`, deploy, or approve pipelines unless explicitly asked.

After pushing, report the branch name, the commit hash, and the pull-request URL if the remote provides one.
